# Core backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Free AI providers (install all — server picks whichever key you set)
google-genai>=0.8.0          # Gemini 2.0 Flash — FREE (aistudio.google.com)
groq>=0.9.0                  # Groq Llama 3.1   — FREE (console.groq.com)

# Paid optional
anthropic>=0.25.0            # Claude Sonnet    — PAID

python-dotenv